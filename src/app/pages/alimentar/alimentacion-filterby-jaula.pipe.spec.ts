import { AlimentacionFilterbyJaulaPipe } from './alimentacion-filterby-jaula.pipe';

describe('AlimentacionFilterbyLineaPipe', () => {
  it('create an instance', () => {
    const pipe = new AlimentacionFilterbyJaulaPipe();
    expect(pipe).toBeTruthy();
  });
});
