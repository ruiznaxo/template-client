import { JaulaFilterbyLineaPipe } from './jaula-filterby-linea.pipe';

describe('JaulaFilterbyLineaPipe', () => {
  it('create an instance', () => {
    const pipe = new JaulaFilterbyLineaPipe();
    expect(pipe).toBeTruthy();
  });
});
