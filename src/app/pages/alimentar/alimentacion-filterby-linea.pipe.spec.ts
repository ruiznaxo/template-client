import { AlimentacionFilterbyLineaPipe } from './alimentacion-filterby-linea.pipe';

describe('AlimentacionFilterbyLineaPipe', () => {
  it('create an instance', () => {
    const pipe = new AlimentacionFilterbyLineaPipe();
    expect(pipe).toBeTruthy();
  });
});
